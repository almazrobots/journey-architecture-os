# Mutation report

Target: `scripts/validate_repo.py`. Tests: `tests.test_repository`, `tests.test_validator`. Generated by `scripts/mutation_test.py` (`make mutation`); local only, not run in CI.

**Mutation score: 99.4%** (1797 of 1808 mutants killed, 11 survived; threshold 90%).

A mutant is killed when the test suite fails or times out on it.

## Operators

| Operator | Mutants | Survived |
|---|---|---|
| DictComp | 6 | 0 |
| False | 33 | 0 |
| GeneratorExp | 8 | 0 |
| ListComp | 10 | 0 |
| SetComp | 7 | 0 |
| True | 17 | 0 |
| boolop | 107 | 0 |
| compare | 213 | 0 |
| constant | 95 | 5 |
| dict | 14 | 0 |
| list | 18 | 0 |
| not | 61 | 0 |
| regex | 31 | 0 |
| return | 2 | 0 |
| set | 9 | 0 |
| statement | 188 | 0 |
| string | 799 | 5 |
| tuple | 190 | 1 |

## Surviving mutants

- #46, line 273, tuple literal -> empty: `REPO_FOLDERS = ("skills", "docs", "scripts", "schemas", "examples", "evals", "tests")` — **fixed after the run**: the test that walked `REPO_FOLDERS` read the mutated constant, so an empty tuple ran no cases; it now lists the folders literally. Verified with `--only 46`: killed.
- #318, line 250, string 'level' -> '': `PORTFOLIO_MIRRORED = ("parent_id", "level", "actor_id", "state", "status")` — level is fixed by the journey_id prefix in both registers, so a level mismatch is already reported by the prefix/level rule
- #928, line 1636, string 'sync_conventions' -> '': `spec = importlib.util.spec_from_file_location("sync_conventions", script)` — the module name is only a label for the imported generator
- #1300, line 1703, string 'repository root to valid' -> '': `"--root", type=Path, default=DEFAULT_ROOT, help="repository root to validate"` — help text only
- #1513, line 1701, constant 0 -> 1: `parser = argparse.ArgumentParser(description=__doc__.splitlines()[0])` — help text only
- #1525, line 486, constant 1 -> 2: `target = raw.split("#", 1)[0].split("?", 1)[0]` — maxsplit 1 or 2 yields the same first element
- #1596, line 1205, string '-' -> '': `if not nid.startswith(prefix + "-"):` — slug tokens start with a letter, so a grammatical node ID that starts with the journey key always continues with '-'
- #1731, line 496, constant 1 -> 2: `if target.split("/", 1)[0] in REPO_FOLDERS or target.split("/", 1)[0] in skill_names:` — maxsplit 1 or 2 yields the same first element
- #1733, line 496, constant 1 -> 2: `if target.split("/", 1)[0] in REPO_FOLDERS or target.split("/", 1)[0] in skill_names:` — maxsplit 1 or 2 yields the same first element
- #1778, line 486, constant 1 -> 2: `target = raw.split("#", 1)[0].split("?", 1)[0]` — maxsplit 1 or 2 yields the same first element
- #1806, line 1432, string '-' -> '': `1 for s in stages if any(m == s or m.startswith(s + "-") for m in measured)` — node segments are exactly two digits, so no valid ID extends a stage ID without a '-'

## Verification after the full run

The validator code has not changed since this full run; only tests were added. The one non-equivalent survivor (#46) was re-run with `python3 scripts/mutation_test.py --only 46` and is now killed. Final result: **1798 of 1808 killed (99.4%)**, 10 equivalent survivors, each listed above with its reason.
